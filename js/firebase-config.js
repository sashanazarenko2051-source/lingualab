// Fill this in with your own Firebase project's config to turn on real
// accounts and the weekly leaderboard. Firebase console → your project →
// Project settings (gear icon) → General tab → "Your apps" → the web app's
// SDK setup snippet has this exact object. Free "Spark" plan is enough.
//
// Until this has real values, App.cloud stays disabled and the app works
// exactly as before (fully offline, local profile only, no leaderboard data).
window.APP_FIREBASE_CONFIG = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT.firebaseapp.com',
  projectId: 'YOUR_PROJECT',
  storageBucket: 'YOUR_PROJECT.appspot.com',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID'
};
